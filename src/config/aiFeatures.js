// AI features flag (DEC-P51).
// The single global switch every launcher, both full-cut entry buttons, and
// the dashboard quick-launch panel sit behind. When false they are hidden,
// not greyed; the AI Analysis nav row stays visible with the mark.
// Founder decision 2026-09-02: created ON. Flip to false to ship dark, the
// same way TRIPWIRE_ADVISORY_ENABLED (advisoryConfig.js) ships dark.
export const AI_FEATURES_ENABLED = true;
